pipeline {

    agent {
        label 'windows11_machine'
    }

    stages {

        stage('Repo2 Build') {
            steps {
                bat 'echo Repo_B running'
            }
        }
    }
}